apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: demo
resources:
  - demo-ns.yaml
  - api
  - worker
  - web
#  - argocd  (optional: ArgoCD UI ingress + root-sync resources live in gitops/argocd)